import 'package:flutter/material.dart';

class CVsPage extends StatelessWidget {
  const CVsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CVs'),
      ),
      body: Center(
        child: Text('List of CVs will appear here'),
      ),
    );
  }
}
